# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "reimu-4232-layer"
BBFILE_PATTERN_reimu-4232-layer = "^${LAYERDIR}/"
LAYERSERIES_COMPAT_reimu-4232-layer = "warrior zeus dunfell gatesgarth"
